import React from "react";
import { Text, View } from 'react-native';

const Homy=({navigation}) => {
    return (
    <View style={{ flex: 1, justifyContent: 'center',
     alignItems: 'center' }}>
    <Text>home!</Text>
  </View>
    );
}
export default Homy;
